// deal with 403 prob *
// add a water mark at the end of pdf *
// check if it is legal
// beauify code
// remove all the comments
// add more arguments *
// standardize the code
// write test for the code
// write readme for the code
// prepare demo
// promote all
// check the legality
// add callback funcs
// add custom path as an argument
// prepare readme
// find user friendly msgs
// find more ergonomic method to input and output manga  info
// apt-get install nodejs npm && npm i -g n && n 10.16.0
// (node:13792) UnhandledPromiseRejectionWarning: Error: Please enter a vaild chapter number catch
// add arguments
// validate all inputs
// add multimple inputs
// '65,,5'.split(',').filter(element => element !== '') muti manga and or all of them
//  new pro add docs section
// spinner
// argHelper()//halter for now checck it later
// add validation to everything
// argv to remove watermark
// enable or disable inet new proj
// add ctrl c msg
// check the avail of inet
// Manga not found
// add man for multiplemanga
// makr sure it work with diff env
// try installing it locally and test
// updated showing up twice
// bench mark
// time taken to a command
// add run code
// multi line to single line \n
// final feats
// check if server is reachable or no
// add exit message
// add invalid option
// add opts to fetch all chapters
// list all chapters
// dumped code
// develop an svg icon
// select chapter 1-n

// choices may be defined as an array or a function that returns an array
// var info = mginfo.find(o => o.title === answers[0]);
// new checkbox({
//   type: 'checkbox',
//     message:chalk.redBright.bold('Choose the manga ? :'),
//     choices: mginfo.map(opts => opts.title),
//   }).ask(async answers => {
// console.log(answers)

//   console.log(chalk.redBright.bold( 'Author(s): ')+chalk.greenBright.bold(info.author))
//   console.log(chalk.redBright.bold( 'Updated: ')+chalk.greenBright.bold(info.time))
// console.log(chalk.redBright.bold( 'Latest Chapters: '))
// info.latest.forEach(chs => console.log(chalk.greenBright.bold(chs)))
//   prompt should onl accept 1 option

// console.log(join('./tmp',basename(parse(img).pathname)));
// var chnum = prompt(chalk.redBright.bold('Chapter number : '));
// probs
// process.on('exit',() => console.log(chalk.redBright.bold('Aborted !!')));
// return join('./tmp',basename(parse(img).pathname))
// return join('./tmp',basename(parse(img).pathname))
// console.log(tmpobj.name)
// logUpdate(chalk.redBright.bold('PDF File saved at :'))
//   console.log(mgi.url+'/'+'chapter-'+chnum)
// var info = mginfo.map(mgi => {
//     return mgi
//     } )
// return join('./tmp',basename(parse(img).pathname))
// console.log(tmpobj.name)
// logUpdate(chalk.redBright.bold('PDF File saved at :'))

//   console.log(mgi.url+'/'+'chapter-'+chnum)
// var info = mginfo.map(mgi => {
//     return mgi
//     } )
// improve opt code

const manga = require('./App');
(async () => {
  console.log(await manga('Jujutsu kaisen'))
})()
